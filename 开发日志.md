# 开发日志

## 2026-09-18

### 添加到桌面引导：iOS 与安卓统一成一套（用户拍板）
- **背景**：初版按平台分叉（安卓=添加到桌面 / iOS=添加到我的小程序），用户要求"iOS 和安卓都做成这一个引导"，并明确不加「添加到我的小程序」按钮。
- **改动**：`utils/device.js` 只返回一套文案（添加到桌面 + 3 步 + "菜单里若没有这一项，请在手机设置中允许微信「创建桌面快捷方式」"）；删掉 `isIOS()`、弹层的「✓ 已添加到「我的小程序」」确认态与 `checkIsAddedToMyMiniProgram` 调用（该 API 只能查「我的小程序」，与本引导目标不符）、埋点 `add_shortcut_guide` 去掉 kind 字段；首页轻引导气泡文案随之一致（"点右上角「···」→ 添加到桌面"）。
- **已知悉的风险**：iPhone 微信胶囊菜单里没有「添加到桌面」这一项（系统不提供桌面快捷方式），iOS 用户照引导点会找不到；若要恢复平台分叉，改 `device.js` 一个函数即可。
- **验证**：模拟器弹层渲染与设计稿一致（截图比对），菜单行「📲 添加到桌面」、弹层标题/步骤/tip、首页气泡文案均正确，无异常。

### 审查发现的两处同类残留修复（指数序列日期守卫推广）
- **审查发现**：① 首页指数条（`pages/index/index.js`）与行情页指数卡都是"取最后两根 K 线相除"，没有日期校验，和收益页刚修的是同一个 bug 类；② 收益页的期望日取的是首页缓存的 `actualDate`（**最滞后**那只基金的净值日），某只基金晚公布（如 968 类 T+1）会把期望日拖回昨天，停在昨天的序列又会被判成合法——同一个 bug 换条件复活。
- **改法**：
  - `utils/market-time.js` 新增四个共享 helper 作为单一来源：`isAShareIndex(code)`（A股指数与基金共用日历，港美欧亚不校验）、`prevTradingDay(dateStr)`（`lastTradingDay` 含当天，取上一交易日要用它）、`expectedIndexDay()`（交易日盘中/盘后=今天；交易日 9:30 前与周末节假日=最近收盘日）、`rowsReachDay(rows, day)`。
  - 收益页：三处日期判断全部改走共享 helper（删掉页内 `_expectIdxDay/_isAShareIdx/_rowsReach` 三个局部实现），期望日不再受"最滞后净值日"影响。
  - 首页：`buildCard` 对 A股指数做序列覆盖校验（不达 → 显示 `--` 占位并带 `lastDate`，不显示错的涨跌）；卡片统一带 `lastDate`；`applyIndexCache` 用 `lastDate` 判"这份缓存是不是在当天 K 线还没出现时写的"，是则不当作新鲜、触发重拉。
  - 行情页**无需改**：合规整改后其指数列表只有港/美/欧/亚指数（无 A股指数），港美欧亚各有各的日历，展示"最近一个交易时段"的涨跌本就是正确语义。
- **验证**：① `market-time` 纯函数单测（expectedIndexDay=今天、prevTradingDay 跨周末/国庆、rowsReachDay 四种输入）；② 首页注入"lastDate=09-17 的旧指数缓存"→ `applyIndexCache` 返回 `false`（不再免拉）→ 按正常流程重进页面后指数卡全部拿到 09-18 数据、缓存重写为 `lastDate=09-18`；③ 注入"lastDate=09-18 的当日缓存"→ 正常复用免拉；④ 收益页三条守卫回归全通过（旧序列自愈 +1.06%、旧缓存被拒、只有旧序列时隐藏对比）；⑤ 10 个页面冒烟无异常。

### 收益页「沪深300 今日涨幅」显示成昨天的（-0.45%）——根因 + 三层日期守卫
- **现象**（用户报，手机截图）：今日复盘卡「组合 +2.2% vs 沪深300 **-0.45%**，跑赢 2.65%」；而沪深300 当日实际 +1.06%。
- **复现**：给页面塞一份"最后一行停在 09-17"的沪深300 序列 → 卡片立刻变成 -0.45% / 跑赢 2.65%，与截图完全一致。−0.45% = 4460.156/4480.268−1 = **09-17 对 09-16** 的涨跌。
- **根因**：今日分支直接取序列**最后两个收盘价**相除，没有校验"序列是否覆盖到组合数据所属日"。而指数序列的两层缓存都可能停在旧日期：
  1. `idx_hist_cache` 只看写入时间是否在收盘后（`isCacheFresh({finalAtClose:true})`）——**早上写的缓存**（那时今天这根 bar 还没出现）会被当成"收盘定格的最终数据"长期复用；
  2. 整页缓存的指数部分（`im`）跟着**基金净值日**判新鲜——晚上净值一公布，整份缓存（含那份旧指数序列）被判为新鲜、免拉全量，`_fetch` 不再跑。
  于是"今日"永远显示昨天的涨跌（首页指数条走的是另一条实时链路，所以只有收益页这个数不对）。
- **修复（三层守卫，纯客户端）**：
  1. **今日分支日期校验**：`_expectIdxDay()`（组合数据所属日 = 首页/本页缓存的 `actualDate`，缺则最近交易日）→ 序列最后一行没到这天就先重拉一次；重拉仍缺 **不显示对比**（沪深300 那格与"跑赢/跑输"整块隐藏），宁可空着也不给错数；
  2. **`_idx` 缓存校验**：A股指数（000001/399001/000300/399006，与基金共用交易日历）额外要求"序列已覆盖到数据日"，否则视为缓存过期重拉；港美欧亚指数日历不同，保持原时间戳判断；
  3. **整页缓存校验**：`_fromCache` 发现 `im` 里的 A股指数没到缓存 `actualDate` → 置 `_idxStale`，不再免拉全量，强制 `_fetch()` 自愈。
- **验证**：① 内存旧序列 → 自动重拉得 +1.06%（原 -0.45%）；② 旧 `idx_hist_cache` → `_idx` 返回的最后一行 09-18（不再吃旧缓存）；③ 上游只给到 09-17 → 卡片隐藏对比（hs=null、无"跑赢"）；④ 整页缓存旧指数 → `_idxStale=true` → 全量刷新后卡片 +1.06%、缓存补到 09-18；四个 tab（今日/本周/本月/本年）全部正常、无异常。

### 我的页：意见反馈 / 数据迁移 表单改为就地展开
- **现象**（用户报）：点「意见反馈」「数据迁移」后表单在**页面最底部**出现（三张表单都堆在全部菜单组之后），点完还得往下找。**要求：点开后紧跟那一行下面展开。**
- **改动**：三张表单（意见反馈 / 数据迁移 / 收盘播报订阅）从页面底部搬进「通知与数据」菜单卡片内、各自紧跟触发行；`.feedback-form`（白卡片）→ 新类 `.menu-expand`（卡片内展开面板：上分隔线 + 内边距，不再自带白底圆角）。收盘播报同款问题一并挪了（同一套交互，不然三个入口行为不一致）。
- **验证**：模拟器实测三个面板纵向位置 = 对应行 top + 57px（正好贴在该行下方），截图确认；页面底部不再有表单堆积。

### 播报金额与页面对不上（+15207 vs +15212.41）：改为净值差口径
- **现象**（用户报）：收到「温度数据通知 +15207元(+2.20%)」，页面今日估算收益 +15,212.41，差 5.41 元。
- **根因**：播报金额 = 「昨日市值 × **官方公布涨幅**」，而官方涨幅只保留 **2 位小数**；乘上几十万市值后单只就能偏 3 元（007491：4.26% vs 精确 4.2634% → −3.01 元；001475 → −2.98 元），12 只累计 −5.4 元。页面走的是「(今日净值 − 昨日净值) × 份额」的精确口径。复算证据：Σ(涨幅×昨日市值) = 15,207.31 → 整数 15,207 = 收到的推送；Σ(净值差×份额) = 15,212.41 = 页面值。
- **修复（dailyBriefing，已部署）**：`fd.fetchLatestNavEastMoney` 本来就返回 `actualNav`，此前只存了涨幅；现在 `navInfo` 增存 `nav`，`resolveRate` 与 `runNavBrief` 内联算式都改为 `(今日净值 − 昨日净值) × 份额`，拿不到净值才退回「涨幅 × 昨日市值」。**两条播报路径（收盘小结 closing_brief / 净值确认 nav_brief）同病，一起改了**。
- **验证**：① 独立复算（同一东财数据源 + 页面份额）Σ(净值差×份额) = 15,212.41，与页面分毫不差；② `dailyBriefing` 以 `dryRun` 跑一遍（不发送），写入的文案为 **`+15212元(+2.20%)`**，与页面一致（整数元是模板 20 字符限制下的取整）。
- 口径约定延续 9-18 那条：**金额一律用能对上的原始量算（净值差×份额），别拿"已公布的百分比"当金额的算料**。

### 合计口径统一：全部 / 账户 / 分组 的金额与收益率对不上（根因 + 修复）
- **现象**（用户报）：只有一个「支付宝」账户，「全部」tab 资产卡 市值 707,364.73 / 累计 +145,864.23，切到「支付宝」tab 变成 707,364.74 / +145,864.24，差 1 分。
- **根因**：同一个组合有两套求和口径，差别只在舍入落点——
  - A（服务端**总额** → 首页「全部/账户汇总」卡）：逐笔用**未舍入**的 `shares×最新净值` / `buyPrice×shares` 累加，最后统一 `toFixed(2)`；
  - B（服务端平台/分组汇总、客户端 `_sumHoldings`、收盘播报 `dailyBriefing`）：逐笔**先舍入到分**再相加。
  - 单账户时两者本应相等，但 A−B = 逐笔舍入残差的累计。实测该账号 12 笔残差合计 **−0.0061 元**，正好跨过 0.005 进位边界 → 显示成差 1 分。（复算证据：Σ逐笔舍入值 = 707,364.74 = 支付宝 tab；Σ未舍入 shares×nav = 707,364.73 = 全部 tab。）
- **修复（getPortfolio，仅云函数）**：
  - 组合总额改为逐笔已舍入值相加：`totalAmount/totalReturn/todayProfit = enriched.reduce(parseFloat(逐笔字段))`，并删掉 `totalMarket/totalCost/totalYesterdayMarket/totalTodayProfit` 这套未舍入累加器；
  - 收益率统一成客户端同式：昨日市值 = 市值 − 今日收益，成本 = 市值 − 累计收益（今日涨幅、累计收益率两个 tab 也不再差）；
  - 顺带统一平台/分组 map：去掉「净值/(1+涨幅) 反推昨收」「buyPrice×shares 当成本」两套老式子，改用同一套 → 账户汇总行的收益率与点进该账户后卡片上的收益率一致。
- **口径约定**：金额合计一律「逐笔先舍入到分，再相加」——用户把列表里的数加起来就能对上合计；不要再引入未舍入量累加后统一舍入的写法。
- **验证**：部署后模拟器实测「列表逐笔相加 / 全部卡 / 平台汇总 / 支付宝卡 / 账户汇总行」五处：市值 707,364.74、累计 +145,864.24、今日 +15,212.41、涨幅 +2.20%、累计率 25.98%，全部一致；收益走势 / 资产分析 / 持仓体检 / 自选页抽查无异常。**纯云函数改动，无需重新发版。**

### 添加到桌面 / 添加到我的小程序 引导（按平台分）
- **背景**：小程序无法自己把图标加到手机桌面，也没有 API 能检测是否已添加（安卓桌面状态完全查不到），只能引导用户走微信右上角胶囊菜单。
- **平台差异（实测 + 文档）**：安卓微信 `···` 菜单里有「添加到桌面」（需系统允许微信「创建桌面快捷方式」）；**iOS 系统不提供桌面快捷方式**，菜单里没有该项，只能引导「添加到我的小程序」（微信首页下拉直达）。
- **新增 `utils/device.js`**：`isIOS()` + `addShortcutGuide()`（返回 label/title/steps/tip/kind），首页气泡与「我的」页入口共用一套文案，避免两处说法分叉。
- **入口**：「我的」页「通用」组新增「📲 添加到桌面」（iOS 显示为「添加到我的小程序」）→ 弹层含右上角胶囊示意（`···` + 圆圈）+ 分步说明 + 平台提示；首页原有轻引导气泡（分享卡保存后触发）文案改为按平台给。
- **已添加检测**：`wx.checkIsAddedToMyMiniProgram` 可用（返回 `{added}`），iOS 已添加过就直接显示「✓ 已添加到「我的小程序」」不再重复引导；老基础库无此 API 时照常给步骤（已做 typeof 守卫）。**注意：网上流传的 `wx.addShortcut` 在 SDK 3.17.3 里是 undefined，不存在，别照抄。**
- **验证**：模拟器 —「我的」页入口 → 弹层（安卓步骤态）、iOS 步骤态、iOS 已添加确认态三种渲染 + 首页气泡文案，均通过、无异常。

### 关注公众号二维码（我的页）
- **背景**：小程序内不能直接跳公众号主页，原实现只有「复制号名 → 去微信搜一搜」一条路（转化差）。
- **改动**：公众号二维码存 `images/official-account-qr.png`（注意：`images/qrcode.jpg` 是分享卡里的小程序码，两者别混）；我的页关注条点击改为弹出二维码卡片——`<image show-menu-by-longpress>` 支持长按「识别图中二维码」（基础库 2.13+，项目 3.3.4），同时长按菜单自带保存图片，无需相册权限；卡片内保留「搜不到？复制公众号名称」作为兜底（原路径不丢）。埋点沿用 `follow_mp`，加 `src: qr|copy` 区分入口。
- **验证**：模拟器点击弹层/复制/关闭三步通过，无异常。

### 审核整改：去掉登录墙，改静默登录（打开即用）
- **审核意见**：打开小程序一进入【持仓等】页面，未浏览体验功能服务即要求授权登录，要求「用户体验浏览功能服务后，再自行选择授权登录」。
- **定位**：全站其实**没有任何手机号/头像/昵称授权**——`userLogin` 云函数只做一件事：从微信调用上下文取 `OPENID` 返回（`cloud.getWXContext()`，不需要用户同意）。真正的问题是客户端把「拿到 openid」包装成一个必须先点的「登录」步骤：`pages/index/index.wxml` 在 `!isLoggedIn` 时用整页空态（"登录后同步你的持仓与分组" + 去登录）顶掉整个持仓页，而持仓页正是小程序首屏。
- **整改（客户端）**：
  - `app.js` 新增 `ensureLogin()`：静默调 `userLogin` 取 openid 写入 `userInfo`（合并写入，不覆盖本地头像/昵称），`onLaunch` 预取；在途 promise 去重，失败清标记供下次重试。
  - **首页**：删掉未登录墙分支；`onShow` 拆成 `_enterPortfolio()`（未登录则静默登录后进正常流程）+ `_loadPortfolio()`（原已登录逻辑）。静默登录失败的极少数情况给「网络异常，点击重试」，下拉刷新即重试登录。截图添加/分享落地 CTA 不再跳登录页。
  - **自选页**：未登录分支同样改为静默登录后加载（`_enterList()`），失败给「加载失败」。
  - **我的页**：删「点击登录 / 登录后可管理持仓数据 / 退出登录」；头部保留头像（本地选择，可选）与「微信用户」，并在头部卡内加一行**账号同步行**（用户拍板形态）：已同步显示「账号 微信账号已同步 ›」（点开说明同步范围），未同步（首启断网等）显示「账号 点击登录 ›」点了即完成登录。数据迁移提交前先确保静默登录。
    - 注：不保留「退出登录」——openid 自动获取后它只是清本地缓存，下次进页面会自动同步回来，用户会以为退出没生效。
  - **删除登录页** `pages/login/index`（连同 app.json / sitemap.json 里的登记），全站不再有「登录」二字出现在用户可见 UI。
- **验证（开发者工具自动化）**：清空本地存储模拟全新用户 → 首页 700ms 瞬态无登录墙、随后自动拿到 openid（`userInfo` 自动写入）并渲染持仓；无持仓时的首屏空态为「添加你的第一笔持仓」（截图添加/搜索添加）；我的页无登录/退出入口；自选页 24 条正常；12 个页面冒烟 0 异常。
- **未改**：云函数侧 `if (!OPENID) return 401` 守卫保留（OPENID 恒有，用户不可见）；CHANGELOG 未动（发版时按惯例另记）。

## 2026-08-22（二）

### 全站合规文案整改（防二次下架）
- **背景**：老小程序因「个人开发者涉金融」被下架，现主体已换个体户。全站审查后把仅剩的定性判断词全部改为数据化/测量化表述，确保每一句用户可见文案都能自证「数据展示工具」。
- **温度措辞**：低估/正常(合理)/高估 → 温度偏低/适中/偏高。改了源头 `_shared/fund-temperature.js`（新数据）、三个读侧（fetchFundEstimate / fetchFundOverview / getPortfolio 加 `ft.sanitizeLabel()` 兼容 DB 旧数据立即生效）、前端 6 处展示（首页列表/tooltip/预警条、详情页、资产分析、运营助手页）。
- **止盈止损 → 涨跌提醒**：设置弹窗文案改「上涨提醒线(%)/下跌提醒线(%)」「温度变化提醒」，功能零改动。
- **简报文案**（opsTool briefing）：「低估代表：XX」改为「估值温度低于 0.75：XX」——纯数据陈述，不再出现定性词+基金名单的组合。
- **费用黑洞**：「假设投资¥100,000，年化收益8%」→「假设本金 ¥100,000，按年化 8% 演算」（规避「年化收益」敏感词）。
- DB 的 signal 枚举（low/mid/high）不动；云函数日志/注释里的旧词保留（不对用户展示）。
- `npm run sync:shared` 已同步 27 个函数；已部署：computeFundTemperature、fetchFundEstimate、fetchFundOverview、getPortfolio、opsTool。

## 2026-08-22

### 全局缓存按交易日时钟重构（含 2026 节假日表）
- **背景**：基金数据只在交易日变化，但各页缓存 TTL 用的是自然时钟（盘外 30min/2min 固定过期），导致周末/节假日/晚间反复拉同样的数据；详情页跨日缓存还被 `sameDay` 拦截不渲染，用户每天第一次进详情必看「加载中」。
- **新增 `utils/market-time.js`**（全局交易日时钟）：
  - 2026 节假日表（国办发明电〔2025〕：元旦/春节 9 天/清明/劳动节/端午/中秋/国庆，只记工作日休市日）；表外年份退化为「工作日=交易日」——宁可多拉一次，不可把交易日错标休市导致数据不刷新。每年 11 月新安排发布后往 `HOLIDAYS` 表补一年即可。
  - 北京时间三态：`trading`（交易日 9:30~15:00）/ `afterClose`（交易日 15:00 后）/ `closed`（非交易日 + 交易日早盘）。
  - `isCacheFresh(cache, opts)` 统一新鲜度：盘中 `estimateTtl`（60s/30s）；盘后净值发布（`actualDate`=今天）即冻结；周末/节假日/早盘 `actualDate`=最近交易日即冻结（零请求）；`finalAtClose` 选项供指数行情这类 15:00 定格数据（无需等净值发布）。
- **各页接入**：
  - **fund-detail**：删 `sameDay` 拦截——跨日/周末缓存直接渲染秒开，`isCacheFresh` 决定是否后台轻刷；轻量刷新补了断档检测（隔多日再进时单点合并补不齐，自动后台全量补拉历史）。
  - **首页**：`portfolio_cache` 保存时写入组合级 `actualDate`（持仓最大净值日），晚间净值发布后即冻结；`index_cache`（指数）`finalAtClose` 收盘后免拉。老格式缓存自动降级（多拉一次后进入精确模式）。
  - **watchlist**：onShow 刷新加市场冻结判断；本地 `isTradingTime` 改走交易日历（国庆等节假日轮询不再空转）；缓存补 `ts`/`actualDate`。
  - **profit-detail**：盘外 TTL 从 2min 改为交易日冻结（该页最重：全量历史+4 指数，周末原每 2 分钟拉一次）；缓存补 `actualDate`（日序列最新日）；`_isTradingNow` 星期兜底改交易日历。
  - **correlation-matrix**：`portfolio_cache` 5min 固定窗口改 `isCacheFresh`；30 天季报缓存不动。
- **getPortfolio 云函数**：持仓返回补 `actualDate` 字段（客户端精确冻结用），已单独部署。
- **效果**：详情页跨日秒开无「加载中」；周末/节假日全天零行情请求；工作日晚间净值发布后停止刷新（原每 30s~2min 一次）。
- **待办**：2026 年 11 月国务院发布 2027 放假安排后，向 `HOLIDAYS` 补 2027 表；小程序端改动需随下次发版生效。

## 2026-08-21

### 运营增长 P0：分享裂变闭环 + 运营助手（推广 Agent）
- **背景**：2,326 用户 / 1,000 DAU，增长不理想。走 P0 裂变闭环：把已有分享行为变成拉新入口。
- **分享链路闭环**（首页 `index.js`）：
  - 数据就绪后自动调 `opsShare createToken` 生成分享令牌（7 天有效，含今日收益/收益率/累计收益/持仓数/基金列表摘要，绑定 openid，登出/换号自动清除），缓存到 Storage。
  - `onShareAppMessage` 动态标题（"我今日收益 +X 元，你的基金温度多少？"）+ path 携带 `share=令牌`。
  - 被分享者打开首页 `onLoad` 解析 `share` 参数 → `opsShare getCard` 拉摘要 → 顶部展示「XX 的持仓」落地横幅（收益 + 持仓基金）→「我也要算我的收益」按钮引导登录/搜索页。
- **渠道小程序码**（`opsTool` + 运营助手页）：
  - `cloud.openapi.wxacode.getUnlimited` 生成带 scene（`c_渠道ID`）的小程序码 → 云存储 → 管理页展示。
  - 首页 `onLoad` 解析 `options.scene` → `opsShare trackVisit` 上报（同用户同渠道每天一次）。
- **温度简报**（`opsTool briefing`）：读当日 `fund_temperatures` 分布（低估/合理/高估）+ 代表基金 → 生成可复制文案（发朋友圈/社群引流钩子），运营助手页一键生成 + 复制。
- **运营助手隐藏页** `pages/ops/index`：管理员 OPENID 白名单（`ops_admins` 集合，首次打开自动登记），sitemap disallow；入口在「我的」页仅管理员可见。
- **新增云函数**：`opsShare`（createToken/getCard/trackVisit）、`opsTool`（checkAdmin/registerAdmin/addAdmin/briefing/listChannels/addChannel/removeChannel/createCode）。
- **新集合**：`share_tokens`、`promo_channels`、`promo_visits`、`ops_admins`（createCollection 已扩展）。
- **部署**：`cli cloud functions deploy` 已完成（opsShare/opsTool/createCollection 全部成功）。
- **小程序码更新**：分享卡片内嵌二维码由旧码替换为新码（`design/gh_a0366a99f578_258.jpg` → `miniprogram/images/qrcode.jpg`，文件名不变，shareCard.js 零改动）。
- **待办**：小程序发版后渠道码/分享链路才在正式版完整生效；简报暂未含指数行情（后续可加）。

## 2026-05-26

### 基金详情页加载性能优化
- **问题**：详情页首次加载 ~26s+，主要瓶颈在 `fetchFundNAVHistory` 串行获取 260 条记录（13 页 × ~2s/页）。
- **修复**：
  1. **减少初始 NAV 获取量**：默认请求从 260 条降到 80 条（4 页，~8s），覆盖近一月和近三月视图。切换到近半年/近一年时按需获取更多。
  2. **轻量级 checkHolding**：替换 `api.getPortfolio()`（获取全部持仓实时行情）为客户端直查 `holdings` 数据库，用 `fetchEstimate` 已有净值数据计算持仓收益。
  3. **懒加载 profile**：`fetchProfile()` 移出 `fetchAll()`，仅在切换"持仓"或"档案"标签时按需加载。
  4. **云函数参数语义化**：`fetchFundNAVHistory` 参数 `pageSize` → `days`（默认 80），超时 15s → 30s。
- **影响**：首次加载从 ~26s 降至 ~8s（快约 3 倍），消除了 `getPortfolio` 的 2N 个无关 HTTP 请求。
- 云函数新增 `SPX`（标普500）、`IXIC`（纳斯达克）指数支持，数据源为新浪美股实时行情（`gb_inx`、`gb_ixic`）。
- Sina US 格式与 A 股不同（`name, price, change_pct, time, change, open, high, low, ...`），新增 `fetchSinaUSQuote()` 专用解析器。
- **已部署**。

### 指数自定义编辑功能
- 指数栏右上角"编辑"按钮，点击展开编辑面板，支持勾选/取消勾选 8 个指数（上证、深证、沪深300、创业板指、恒生科技、恒生指数、标普500、纳斯达克）。
- 选择偏好保存到 Storage（`indexCodes`），下次启动自动加载。
- 收起态显示第一个选中指数，展开态横向滚动全部选中指数。
- 编辑面板高度 420rpx，不影响 scroll-view 底部 padding 自适应。
- "完成"按钮关闭面板不保存，"保存"按钮提交并刷新指数数据。

### fetchMarketIndex 云函数重写（实时行情 + 港股多源兜底）
- **问题**：A 股指数显示昨天收盘数据而非实时价格；港股指数（HSTECH、HSI）无数据。
- **修复**：
  - A 股：K 线数据 + 新浪 JS 实时行情（`hq.sinajs.cn`）并行获取后融合，当日数据用实时价格覆盖，新交易日追加数据点。
  - 港股：7 级回退链——东方财富实时 → 新浪实时 → 腾讯实时 → 东方财富全球 K 线 → 新浪港股 K 线 → 腾讯港股 K 线 → Yahoo 兜底。
  - 新增 `fetchEastMoneyRealtime()`、`fetchTencentRealtime()`、`buildQuoteData()`、`formatDate()` 辅助函数。
  - 实时行情接口自动构建 2 日伪 K 线（昨收 + 当前价）。
- **已部署**。

### 港股指数数值全 0 修复
- **问题**：港股指数（HSTECH、HSI）显示了但数值全为 0。
- **根因**：数据源优先级把不可靠的东方财富实时 API 放第一位，且 `fetchTencentRealtime` 硬编码 `prevClose=0`、`open=price`，未解析腾讯 API 的实际字段。
- **验证**： curl 确认新浪（`hq.sinajs.cn`）港股指数无数据（空响应），东方财富（`push2.eastmoney.com`）超时，腾讯（`qt.gtimg.cn`）正常返回完整数据（HSTECH: 4946.88, HSI: 25599.45）。
- **修复**：
  - 港股数据源调整为：腾讯实时 → 东方财富实时 → 新浪实时 → 腾讯 K 线 → 东方财富全球 K 线 → 新浪港股 K 线 → Yahoo。
  - `fetchTencentRealtime` 正确解析 `parts[4]`（昨收）、`parts[5]`（开盘）并传入 `buildQuoteData`。
- **已部署**。

### 首页指数栏收起态间距修复（第二次尝试）
- `ic-col` 从 `text-align` 方案改为嵌套 flex：`flex: 1; display: flex` + `justify-content`（flex-start/center/flex-end），让三列在 WeChat 中真正等宽分布。

## 2026-05-24

### 指数对比图修复
- **问题**：`push2his.eastmoney.com` K线接口持续返回 500，导致收益走势页指数对比图无法渲染。客户端 `wx.request` 和云函数 `https.get` 均无法访问该域名（其他东方财富子域名正常）。
- **修复**：`fetchMarketIndex` 云函数主数据源切换为新浪财经 API（`money.finance.sina.com.cn`），东方财富作为备用。新浪使用 `scale=240`（日K线），数据字段映射为 `date/open/close/high/low/volume`。
- **影响**：`cloudfunctions/fetchMarketIndex/index.js` 重写，已部署。

### getPortfolio 云函数性能优化
- 持仓查询从串行 `for` 循环改为 `Promise.all(holdings.map(...))` 并行请求，每个持仓同时发起天天基金 + 东方财富 + NAV历史（可选）请求。
- 新增 `historyDays` 参数，支持一次调用返回 NAV 历史数据（`navHistoryMap`），减少客户端多次调用。
- `fetchNAVHistory` 内置到云函数中，分页拉取（东方财富 `api.fund.eastmoney.com`）。

### 收益走势页性能优化
- 不再对每个持仓单独调 `fetchFundNAVHistory`，改为从 `getPortfolio(80)` 一次性获取所有 NAV 历史。
- 指数数据与 NAV 数据处理并行执行（`indexPromise`）。
- 基金详情页 `checkHolding()` 合并到 `Promise.all` 并行执行。
- 纵轴改为收益率（%），非对比模式不再显示金额。

### 客户端 fetchMarketIndexClient 增强
- 添加重试机制（最多 3 次，指数退避 800ms/1600ms）+ 备用端点（腾讯财经 `web.ifzq.gtimg.cn`）。
- 请求失败时 fallback 到云函数 `fetchMarketIndex`。

### 首页功能
- 持仓超过 8 个支持"加载更多"分页展示。
- 金额全局显示/隐藏切换（仅隐藏金额数字，百分比和净值不隐藏），偏好保存到 Storage。

### UI 调整
- 今日收益卡片缩小（padding 36rpx，value font 60rpx）。
- 快捷入口缩小（padding 20rpx，icon 36rpx）。
- 日收益金额字体增大到 22rpx。

## 2026-05-25

### 骨架屏加载
- `app.wxss` 新增全局 `.skeleton` 脉冲动画样式（渐变位移模拟闪烁）。
- 全部 6 个页面替换纯文本"加载中..."为骨架占位块：首页、基金详情、收益走势、自选、基金对比、搜索。
- 首页新增 loading 状态管理。

### 下拉刷新
- `fund-detail`、`profit-detail`、`watchlist` 三个页面开启 `enablePullDownRefresh`，添加 `onPullDownRefresh` 方法。

### 空状态引导
- 首页已登录但无持仓时显示引导空状态："暂无持仓" + 跳转搜索按钮。
- 收益走势页无数据时显示空状态："暂无收益数据" + 返回首页按钮。

### 收益计算修复
- **问题**：getPortfolio 云函数代码改了但未重新部署，`navHistoryMap` 未返回导致收益走势页降级到 mock 随机数据，日历出现异常负值。
- **修复**：部署最新 getPortfolio；mock 数据只在零持仓时启用。
- 日历"今天"格子始终标记为空（当日净值未公布）。
- 月/年收益统一用"日变动求和"确保日历与顶部汇总自洽（待商榷）。

### 首页指数行情
- 首页底部新增 A 股主要指数 + 恒生科技指数展示区域，默认收起显示上证指数，点击展开可左右滑动查看全部 5 个指数。
- 云函数 `fetchMarketIndex` 新增 `fetchYahooKline()` 支持恒生科技指数（Yahoo Finance v8 chart API）。
- 客户端 `fetchIndices()` 并行请求 5 个指数各 2 日 K 线，最新 close 为现价，前一日 close 计算涨跌额/涨跌幅。
- 红涨绿跌配色，卡片背景根据涨跌微色调。

### 首页指数行情迭代
- 新增恒生指数（HSI），总计 6 个指数（上证、深证、沪深300、创业板指、恒生科技、恒生指数）。
- 指数栏改为绝对定位悬浮在 tabBar 上方，scroll-view 高度不变避免布局抖动。
- 展开/收起按钮独立一行置于右上角，收起时只显示上证指数，展开时横向滑动全部指数。
- 收起状态下三个数值（名称、现价、涨跌幅）采用 view 三分栏布局（33.33% 宽度），但 WeChat 中 `<text>` 元素 flex 宽度表现异常，间距未完美对齐（待处理）。

### 云函数 fetchMarketIndex 港股指数支持
- HK 指数统一走 `fetchHKIndexKline()`，数据源优先级：新浪实时行情（hq.sinajs.cn）→ 东方财富全球 → 新浪 HK K线 → 腾讯财经 → Yahoo 兜底。
- 新浪实时行情从当前价和昨收价构建伪 K 线数据。
- 已知问题：恒生科技指数（HSTECH）新浪实时行情 `hstHSTECH` 可能无数据（该指数较新，新浪可能未覆盖）。

### 2026-08-10 全面重构（审查 + 4 批重构）
- **批次1 核心链路**：getPortfolio 单只基金数据源全挂不再拖垮整个接口；温度阈值统一 0.75/1.25；修复 weightedPE 恒为 1 的 bug；batchAddHoldings 从已失效的 fundgz 迁移到东方财富；fetchFundEstimate 去掉重复的东财请求并修复 `db` 未定义导致温度缓存写入静默失败的 bug。
- **公共模块**：新增 `cloudfunctions/_shared/fund-data.js` + `fund-temperature.js`（东方财富持仓/净值、腾讯行情、北京时间、温度打分），通过 `npm run sync:shared` 同步进各云函数，消除 5+ 处重复实现。
- **批次2 云函数容错**：删除无调用方的 fetchFundInfo / fetchFundRank / fetchIndexIntraday；searchFund / dcaBacktest / computeCorrelation / batchFetchEstimate 补登录校验；ocrTransaction 的 OCR.space 兜底改为读取 OCRSPACE_API_KEY。
- **批次3 数据库一致性**：manageHolding 增加字段白名单（防 _openid 覆盖）；客户端所有直写数据库改为走云函数；bindMigrationCode 改为幂等迁移（先复制后清理、断点续传去重、迁移码占用标记）。
- **批次4 规范**：AGENTS.md 修正为当前 AppID `wxb95098fe432ed765` / 云环境 `cloud1-d7gu9zv3i796839b8`，移除已下线函数。
- **部署（已通过微信开发者工具 CLI 完成）**：cloudbase CLI 账号不可见新环境 `cloud1-d7gu9zv3i796839b8`，改用 `/Applications/wechatwebdevtools.app/Contents/MacOS/cli cloud functions deploy` 逐个部署 16 个改动函数；云端下载比对 getPortfolio / batchAddHoldings / computeFundTemperature 与本地完全一致。
